# 实施与排查记录

## 媒体资源预览

- `vite-plugin-manus-runtime` 提供的是用于注入浏览器端运行时的 Vite 插件，并不直接为 Astro 开发服务器注册 `/manus-storage/` 静态资源代理。
- 在当前 Astro 预览中，已上传资源的相对路径返回 404。因此需要选择一个能在 Astro 静态构建产物中可靠提供图片的方式，同时保留项目外的原始图片资产。
